default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch('RAILS_MAX_THREADS') { 5 } %>

development:
  <<: *default
  database: falcon_time_development

test:
  <<: *default
  database: falcon_time_test

production:
  url: <%= ENV.fetch('DATABASE_URL') { '' } %>
